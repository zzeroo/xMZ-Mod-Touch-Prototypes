Die `.dot` Dateien können mit den [graphviz](http://www.graphviz.org/) Tools `circo` usw. in .png Dateien konvertiert werden.

Zum Beispiel:

```bash
circo share/example.dot -Tpng -o share/example.png
```
